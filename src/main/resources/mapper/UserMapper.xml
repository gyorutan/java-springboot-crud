<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tpi.springboot.crud.demo.mapper.UserMapper">
    <insert id="insertUser" parameterType="com.tpi.springboot.crud.demo.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password) VALUES (#{username}, #{password})
    </insert>

    <select id="selectUserById" resultType="com.tpi.springboot.crud.demo.domain.User">
        SELECT id, username, password FROM users WHERE id = #{id}
    </select>

    <select id="selectAllUsers" resultType="com.tpi.springboot.crud.demo.domain.User">
        SELECT id, username, password FROM users ORDER BY id ASC
    </select>

    <update id="updateUser" parameterType="com.tpi.springboot.crud.demo.domain.User">
        UPDATE users SET username = #{username}, password = #{password} WHERE id = #{id}
    </update>

    <delete id="deleteUser">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="selectUserByUsername" resultType="com.tpi.springboot.crud.demo.domain.User">
        SELECT id, username, password FROM users WHERE username = #{username}
    </select>

</mapper>